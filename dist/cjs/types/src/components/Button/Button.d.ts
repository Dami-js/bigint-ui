import React from "react";
export interface ButtonProps {
    size?: number;
}
declare const Button: import("styled-components").StyledComponent<(props: React.PropsWithChildren<ButtonProps>) => JSX.Element, any, {}, never>;
export default Button;
